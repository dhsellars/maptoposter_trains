import matplotlib.pyplot as plt\nimport geopandas as gpd\nimport os\n\ndef create_map_poster(output_file, width, height, is_inches=True):\n    # Convert dimensions to centimeters if necessary\n    if is_inches:\n        width *= 2.54  # inches to cm\n        height *= 2.54\n\n    fig, ax = plt.subplots(figsize=(width / 2.54, height / 2.54))  # Convert cm to inches\n\n    # Load some geographical data\n    world = gpd.read_file(gpd.datasets.get_path('naturalearth_lowres'))\n    world.plot(ax=ax, color='lightgrey')\n\n    # Set title\n    ax.set_title('Map Poster', fontsize=24)\n\n    # Save the figure\n    plt.savefig(output_file, bbox_inches='tight')\n    plt.close(fig)\n\n# Example usage of the function\n# create_map_poster('map_poster.png', 24, 36, is_inches=True)  # For 24x36 inches\n# create_map_poster('map_poster_cm.png', 60, 90, is_inches=False)  # For 60x90 cm\n